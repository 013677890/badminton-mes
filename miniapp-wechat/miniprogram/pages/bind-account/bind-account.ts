import { bindAccount } from '../../services/auth'
Page({ data: { ticket: '', userNo: '', password: '' }, onLoad(query: { ticket?: string }) { this.setData({ ticket: query.ticket || '' }) }, input(event: WechatMiniprogram.Input) { this.setData({ [event.currentTarget.dataset.key]: event.detail.value }) }, bind() { const { ticket, userNo, password } = this.data; if (!ticket || !userNo || !password) { wx.showToast({ title: '请填写完整', icon: 'none' }); return } wx.showLoading({ title: '绑定中' }); bindAccount(ticket, userNo, password).then(() => wx.reLaunch({ url: '/pages/dashboard/dashboard' })).catch(error => wx.showToast({ title: error.message, icon: 'none' })).finally(() => wx.hideLoading()) } })
